using UnityEngine;

public class GameManager : MonoBehaviour
{
    void Start()
    {
        FadeManager.FadeIn(0.3f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
